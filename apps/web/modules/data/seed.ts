import type { Artist, Pledge, Project, Support } from "@antiq/types";

/** Empty catalog — populate via Create artist / Add project. */
export const artists: Artist[] = [];

export const initialProjects: Project[] = [];

export const initialPledges: Pledge[] = [];

export const initialSupports: Support[] = [];
